import random
random.choice(['apple', 'pear', 'banana'])  # random choice
random.sample(range(100), 10)       # sampling
random.random()     # random number [0-1]
random.randrange(6) # random int choosed from range(6)

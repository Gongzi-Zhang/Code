#!/usr/bin/awk

# single quote
# it looks like there is no single quote string in awk
# even the single quote itself are not allowed, if you want to print
# a single quote, you have to use \047 or set it as field separator
